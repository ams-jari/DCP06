# DCP06 Unit Tests

Placeholder for unit tests. To be populated when test framework (e.g. Catch2, Google Test) is integrated.

## Suggested structure

- `tests/unit/` — unit tests for core logic, calculation, file I/O
- `tests/integration/` — end-to-end tests (when build supports it)
